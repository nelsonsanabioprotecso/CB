# Docs for the Azure Web Apps Deploy action: https://github.com/azure/functions-action
# More GitHub Actions for Azure: https://github.com/Azure/actions
# More info on Python, GitHub Actions, and Azure Functions: https://aka.ms/python-webapps-actions

name: Build and deploy Python project to Azure Function App - CB-AMEX

on:
  push:
    branches:
      - main
<<<<<<< HEAD

env:
  AZURE_FUNCTIONAPP_NAME: 'CB-AMEX'  
  AZURE_FUNCTIONAPP_PACKAGE_PATH: '.'
  PYTHON_VERSION: '3.11'             
=======
  workflow_dispatch:

env:
  AZURE_FUNCTIONAPP_PACKAGE_PATH: '.' # set this to the path to your web app project, defaults to the repository root
  PYTHON_VERSION: '3.11' # set this to the python version to use (supports 3.6, 3.7, 3.8)
>>>>>>> 313bb8dbccddc3e207a728f0289ccc5039c7d87f

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

        #- name: Create and start virtual environment
        #run: |
        #python -m venv venv
        #source venv/bin/activate

    - name: Install dependencies
      shell: bash
      run: |
        pushd './.'
        python -m pip install --upgrade pip
        pip install -r requirements.txt --target=".python_packages/lib/site-packages"
        pip install azure-functions --no-cache-dir
        pip list
        popd
        pwd

    - run: |
        python function_app.py 
        pwd

    - name: Setup Node.js environment
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: List files to be zipped
      run: ls -la

    - name: Zip the application for deployment
      run: |
        zip -r release.zip . -x "*.git*" -x ".github/*" -x "*.python_packages/*" -x "venv/*"

    - name: List and Check the size of the zip
      run: |
        ls -lh release.zip
        unzip -l release.zip

    - name: Upload ZIP as Artifact
      uses: actions/upload-artifact@v4
      with:
        name: functionapp-artifact
        path: |
          ./release.zip
          !venv/

  deploy:
    needs: build-and-deploy
    runs-on: ubuntu-latest
    environment: production
    steps:
    - uses: actions/download-artifact@v4
      with:
        name: functionapp-artifact

    - name: List Release.zip
      run: |
        ls -la
        unzip -l ./release.zip
        pip list

    - name: Deploy to Azure Functions
      uses: Azure/functions-action@v1
      id: fa
      with:
        app-name: ${{ env.AZURE_FUNCTIONAPP_NAME }}
        package: release.zip
        publish-profile: ${{ secrets.AZURE_FUNCTIONAPP_PUBLISH_PROFILE }}
        scm-do-build-during-deployment: false
        enable-oryx-build: true
=======
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python version
        uses: actions/setup-python@v1
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Create and start virtual environment
        run: |
          python -m venv venv
          source venv/bin/activate

      - name: Install dependencies
        run: pip install -r requirements.txt

      # Optional: Add step to run tests here

      - name: Zip artifact for deployment
        run: zip release.zip ./* -r

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v3
        with:
          name: python-app
          path: |
            release.zip
            !venv/

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-function.outputs.webapp-url }}
    permissions:
      id-token: write #This is required for requesting the JWT

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v3
        with:
          name: python-app

      - name: Unzip artifact for deployment
        run: unzip release.zip     
        
      - name: Login to Azure
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_C469E01B484F4C179ADE114A47339EF3 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_FD5EEB44235044E2AFC6C39DFC8C29CA }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_781D307756CB48F99A59A09A9601EE32 }}

      - name: 'Deploy to Azure Functions'
        uses: Azure/functions-action@v1
        id: deploy-to-function
        with:
          app-name: 'CB-AMEX'
          slot-name: 'Production'
          package: ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
          scm-do-build-during-deployment: true
          enable-oryx-build: true
          
>>>>>>> 313bb8dbccddc3e207a728f0289ccc5039c7d87f
